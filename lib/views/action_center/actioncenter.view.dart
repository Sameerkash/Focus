import 'package:flutter/material.dart';

class ActionCenterView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ActionCenter"),
      ),
    );
  }
}
